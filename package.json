{
  "name": "webar-floor-placement",
  "version": "1.0.0",
  "description": "Lightweight browser-based AR experience with WebXR floor detection and dynamic model loading",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "serve": "vite preview --host",
    "postinstall": "npm run build",
    "start": "npx serve -s dist -l $PORT"
  },
  "dependencies": {
    "aframe": "^1.5.0"
  },
  "devDependencies": {
    "vite": "^5.4.11",
    "@vitejs/plugin-basic-ssl": "^1.1.0"
  },
  "author": "",
  "license": "MIT"
}
